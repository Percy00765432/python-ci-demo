name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      # Add build/test steps here if needed
      - name: Run something
        run: echo "CI running..."